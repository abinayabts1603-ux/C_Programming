//Consonant-or-Not

#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */   
    char ch;
    scanf("%c",&ch);
    if(ch == 'A' || ch == 'a' || ch == 'E' || ch == 'e' || ch == 'I' || ch == 'O' || ch == 'o' || ch == 'U' || ch == 'u' || ch == 'Y' || ch == 'y' || ch == '1' || ch == '2' || ch == '3' || ch == '4' || ch == '5' || ch == '6' || ch == '7' || ch == '8' || ch == '9')
        printf("Not Consonant");
    else
        printf("Consonant");
    return 0;
}
